# ===============================================================
#       AI News Bot Environment Configuration Example
# ===============================================================
# Copy this file to .env (command: cp .env.example .env) and fill in your values.

# --- Telegram Bot Configuration ---
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN_HERE"

# Target Channel or Group ID (must be numeric, e.g., -100123456789)
TELEGRAM_CHAT_ID="YOUR_TARGET_CHANNEL_OR_GROUP_ID"

# --- AI (Artificial Intelligence) Configuration ---
# Get your API key from Google AI Studio
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# The prompt that the AI will use to summarize the news.
# You can change the tone of the summary by editing this prompt.
AI_PROMPT="You are a professional news editor. Create a short, dense, and engaging one-sentence summary in English for the following news excerpt. Ignore irrelevant information. Here is the news:"

# --- News Bot Configuration ---
# Path to the JSON file containing the list of news sources.
NEWS_SOURCES_FILE_PATH="sources.json"

# Default language for bot messages (e.g., /start command).
# Use "id" for Indonesian, "en" for English.
DEFAULT_LANGUAGE="en"

# --- Scheduling & Throttling Configuration ---
# How often the bot will check for new news (in minutes).
# Example: 60 for 1 hour, 360 for 6 hours.
SCHEDULE_INTERVAL_MINUTES=60

# Maximum number of articles to post in a single check cycle.
# This is useful to prevent spamming if many new articles are found at once.
POST_LIMIT_PER_RUN=5